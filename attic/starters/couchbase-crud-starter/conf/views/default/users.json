{
    "name":"users",
    "views":{
        "by_name": {
            "map": "function (doc, meta) { if (doc.name) { emit(doc.name, null); } } "
        },
        "by_age": {
            "map": "function (doc, meta) { if (doc.age) { emit(doc.age, null); } } "
        }
    }
}